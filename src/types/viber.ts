export interface Button {
        Columns: number;
        Rows: number;
        ActionType: number;
        ActionBody: string;
        Image?: string;
        Text: string;
        TextSize?: string;
        TextVAlign?: string;
        TextHAlign: string;
}